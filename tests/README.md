# Tests

This folder is intentionally scaffolded before adding test cases.

Planned suites:
- `unit/` for pure logic tests
- `api/` for HTTP/API behavior tests
- `eval/` for model quality/regression tests

See `TEST_PLAN.md` for the roadmap and marker strategy.
